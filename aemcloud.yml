---
version: "v2"
branches:
  only:
    - master
    - develop
  exclude:
    - feature/*
build:
  image: node:10
  script:
    - npm install
    - npm test
  artifact_path:
    - test-camel-multi/target
    - ui/target/*.zip
